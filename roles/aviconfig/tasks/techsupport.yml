# Automatically Generated File
# Copyright (c) 2026 Broadcom Inc. and/or its subsidiaries. All Rights Reserved. Broadcom Confidential.
---
- name: Avi TechSupport | Create or Update TechSupport
  no_log: "{{ avi_role_config_log_mode | default(False) }}"
  vmware.alb.avi_techsupport:
    controller: "{{ avi_controller | default(omit) }}"
    username: "{{ avi_username | default(omit) }}"
    password: "{{ avi_password | default(omit) }}"
    tenant: "{{ item.tenant | default(omit) }}"
    state: "{{ avi_role_state_mode }}"
    api_version: "{{ api_version | default(omit) }}"
    avi_credentials: "{{ avi_credentials | default(omit) }}"
    api_context: "{{ avi_api_context | default(omit) }}"
    avi_api_update_method: "{{ item.avi_api_update_method | default(omit) }}"
    avi_api_patch_op: "{{ item.avi_api_patch_op | default(omit) }}"
    case_number: "{{ item.case_number | default(omit) }}"
    description: "{{ item.description | default(omit) }}"
    duration: "{{ item.duration | default(omit) }}"
    end_time: "{{ item.end_time | default(omit) }}"
    errors: "{{ item.errors | default(omit) }}"
    level: "{{ item.level | default(omit) }}"
    name: "{{ item.name | default(omit) }}"
    node: "{{ item.node | default(omit) }}"
    obj_name: "{{ item.obj_name | default(omit) }}"
    obj_state: "{{ item.obj_state | default(omit) }}"
    obj_uuid: "{{ item.obj_uuid | default(omit) }}"
    output: "{{ item.output | default(omit) }}"
    params: "{{ item.params | default(omit) }}"
    progress: "{{ item.progress | default(omit) }}"
    size: "{{ item.size | default(omit) }}"
    start_time: "{{ item.start_time | default(omit) }}"
    tasks: "{{ item.tasks | default(omit) }}"
    tasks_completed: "{{ item.tasks_completed | default(omit) }}"
    techsupport_readiness: "{{ item.techsupport_readiness | default(omit) }}"
    tenant_ref: "{{ item.tenant_ref | default(omit) }}"
    total_tasks: "{{ item.total_tasks | default(omit) }}"
    uuid: "{{ item.uuid | default(omit) }}"
    warnings: "{{ item.warnings | default(omit) }}"
  when:
    - (avi_role_state_mode == "present" and avi_config_state == "present")
    - (item.state | default("present") == avi_role_state_mode or avi_config_state == "absent")
  with_items: "{{ avi_role_cfg.techsupport | default([]) }}"
