# Automatically Generated File
# Copyright 2021 VMware, Inc. All rights reserved. VMware Confidential
---
- name: Avi PasswordPolicy | Delete PasswordPolicy
  no_log: "{{ avi_role_config_log_mode | default(False) }}"
  vmware.alb.avi_passwordpolicy:
    controller: "{{ avi_controller | default(omit) }}"
    username: "{{ avi_username | default(omit) }}"
    password: "{{ avi_password | default(omit) }}"
    tenant: "{{ item.tenant | default(omit) }}"
    api_version: "{{ api_version | default(omit) }}"
    avi_credentials: "{{ avi_credentials | default(omit) }}"
    api_context: "{{ avi_api_context | default(omit) }}"
    state: "{{ avi_role_state_mode }}"
    avi_api_update_method: "{{ item.avi_api_update_method | default(omit) }}"
    avi_api_patch_op: "{{ item.avi_api_patch_op | default(omit) }}"
    configpb_attributes: "{{ item.configpb_attributes | default(omit) }}"
    lockout_evaluation_period: "{{ item.lockout_evaluation_period | default(omit) }}"
    lockout_max_auth_failures: "{{ item.lockout_max_auth_failures | default(omit) }}"
    lockout_period: "{{ item.lockout_period | default(omit) }}"
    min_length: "{{ item.min_length | default(omit) }}"
    min_lowercase: "{{ item.min_lowercase | default(omit) }}"
    min_numeric: "{{ item.min_numeric | default(omit) }}"
    min_special: "{{ item.min_special | default(omit) }}"
    min_uppercase: "{{ item.min_uppercase | default(omit) }}"
    name: "{{ item.name | default(omit) }}"
    password_expiration_days: "{{ item.password_expiration_days | default(omit) }}"
    password_history: "{{ item.password_history | default(omit) }}"
    tenant_ref: "{{ item.tenant_ref | default(omit) }}"
    uuid: "{{ item.uuid | default(omit) }}"
  when:
    - (item.name | length > 0)
    - (not (item.system_default | default(false)))
    - (avi_config_state == "absent" or avi_role_state_mode == "absent")
    - (item.state | default("present") == avi_role_state_mode or avi_config_state == "absent")
  with_items: "{{ avi_role_cfg.passwordpolicy | default([]) }}"
